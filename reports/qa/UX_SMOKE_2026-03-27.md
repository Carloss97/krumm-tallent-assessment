# UX Smoke Report - 2026-03-27

## Scope
- Goal: validate route-level UX continuity for games 1-13 after redesign.
- Environment: local dev full stack (`npm run dev:full`) on Windows.
- Date: 2026-03-27.

## Execution Evidence
1. Critical E2E flow command:
- `npm run test:e2e`
- Result: scenario 1 passed (`E2E scenarios passed`), scenario 2 failed in backend-down fallback.
- Failure: timeout waiting report heading (`skills evaluation matrix`) after backend shutdown.

2. Route smoke command (Playwright inline script):
- Navigated to `/intro` -> quick demo -> explicit route visit `/game/1` to `/game/13`.
- Result snapshot:
  - Games 1-9: route-level progress markers found.
  - Games 10-13: failed route expectation.

3. Diagnostic extraction for games 10-13:
- Body text for `/game/10` to `/game/13` repeatedly shows:
  - `PROGRESO DE EVALUACION`
  - `1 / 13`
  - `Consentimiento para Evaluacion`
- Indicates navigation/state regression (late complementary routes not preserving requested game index).

## Findings By Severity
- Severo:
  - Route regression for game routes 10-13. Requested routes present game-1 consent/progress state (`1 / 13`) instead of expected 10-13 context. Reproducible via direct visit to `/game/10`, `/game/11`, `/game/12`, `/game/13`.
- Moderado:
  - Backend-down fallback E2E scenario times out on report stabilization with repeated API health proxy errors.
- Leve:
  - Consent modal automation is sensitive to checkbox state timing in headless runs; selectors should explicitly target privacy acknowledgment checkbox.

## Reproduction Steps
1. Start stack: `npm run dev:full`.
2. Open intro and trigger demo path.
3. Visit `/game/10` directly.
4. Observe progress/header text showing `1 / 13` + consent screen instead of game 10 context.

## Impact
- Users may be unable to access complementary games 10-13 as intended.
- QA smoke and E2E confidence for final segment of battery is reduced until route-state bug is fixed.

## Recommendation Priority
- Alta:
  - Fix game routing/state initialization for indexes 10-13.
- Media:
  - Stabilize backend-down fallback page readiness check in E2E (avoid strict `networkidle` dependency when API health polling is active).
- Baja:
  - Harden QA smoke helper with deterministic consent handling utility.
