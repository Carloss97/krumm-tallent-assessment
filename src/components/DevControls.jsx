// DevControls intentionally disabled — the floating dev menu was noisy
// and distracting. Return null to remove it from all environments.
import React from 'react';

const DevControls = () => null;

export default DevControls;
